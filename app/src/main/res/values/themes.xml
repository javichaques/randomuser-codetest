<resources>
    <style name="Theme.RandomUser.App" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#FFFFFF</item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="Theme.RandomUser.Splash" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.RandomUser.App</item>

        <item name="windowSplashScreenBackground">#FFFFFF</item>
    </style>
</resources>